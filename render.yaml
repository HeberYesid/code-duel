services:
  - type: web
    name: code-duel
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ALLOWED_ORIGINS
        value: ""
      - key: WS_ALLOWED_ORIGINS
        value: ""
